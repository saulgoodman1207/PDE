function quartic_separable_anchor_test()
% Regression test for a quartic-specific separable anchor.

    opts = struct( ...
        'N_values', [14 18], ...
        'p_values', [15 19], ...
        'run_tag', 'tdd_quartic_separable_anchor', ...
        'show_figures', false);

    result = u0_anchor_monge_ampere_homotopy_demo('quartic_convex', opts);
    assert(all(result.summary_table.Success), ...
        'Expected quartic_convex paired sweep to succeed for N = 14 and 18 with the separable anchor.');
end
